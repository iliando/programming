const length1 = [ `` ]
const length2 = [ `` ]

if (length1 < length2)
{
    alert(`${length1} < ${length2}`);
}
else if (length1 > length2)
{
    alert(`${length1} > ${length2}`);
}
else
{
    alert(`${length1} + ${length2}`);
}